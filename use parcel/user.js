export const zigkie = {
  name: "Zigkie",
  age: 24,
}

export const dab = {
  name: "Dab",
  age: 29,
}

export default function printUser(person) {
  console.log(`Name: ${person.name}. Age: ${person.age}`)
}
